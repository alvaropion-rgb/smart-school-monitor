{
  "name": "sharkquick-cloud-relay",
  "version": "1.0.0",
  "description": "Cloud relay for SharkQuick QR service requests — serves the phone-facing request page when local network access is blocked",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "18.x"
  }
}
